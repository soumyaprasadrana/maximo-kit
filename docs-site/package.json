{
  "name": "maximo-kit",
  "version": "0.1.0",
  "private": true,
  "description": "maximo-kit - Spec Kit extension with recipes, knowledge, MCP workflow",
  "scripts": {
    "validate:ascii": "node scripts/validate-ascii.mjs",
    "validate:repo": "node scripts/validate-repo.mjs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "build:release": "node scripts/build-release.mjs"
  },
  "devDependencies": {
    "adm-zip": "^0.5.18",
    "vitepress": "^1.6.4"
  }
}
